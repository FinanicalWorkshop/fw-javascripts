const utils = require('../core/app-bridge.js')
const assert = require('assert')

describe('AppBridge', function () {
    describe('', function () {
        before(function () {
            global.NativeBridge = {}
        })
        it('should', function () {
        })
    })
})