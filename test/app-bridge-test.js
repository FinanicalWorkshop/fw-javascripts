
import assert from 'assert';
import AppBridge from '../core/app-bridge.js';

describe('AppBridge', function () {
    describe('', function () {
        before(function () {
            global.NativeBridge = {}
        })
        it('should', function () {
        })
    })
})